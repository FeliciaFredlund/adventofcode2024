package days

// func getFileName(day#, star#, example=false)
// dXsX.txt / dXsX_example.txt

// func getFilePath(input/output, filename)
// ./inputs/filename
// ./outputs/filename
// but . is full path

// func saveOutput(data, filename/path, example=false) calls either normally or uses saveExample???

// func saveExampleOutput(data) makes sure that expected result isn't saved over
