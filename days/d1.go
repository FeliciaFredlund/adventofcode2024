package days

//day1s1, day1s2

// day 1: pair up values in two lists, sorted in ascending order
// and get the difference between the numbers
// sum those differences together

// general day function?
// func getInput(example=false) if example = true, just get data
// else checks if there is a cache,
// if yes, get data from cache,
// if not, get the data via url

// func parseInput into two slices
// func getDifference between values
// func Day1(example=false) to do the whole thing, checking if input is cached, etc.
