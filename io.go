package main

// func getDataFromUrl(url, day#) make http request and save to file
// func getDataFromFile(day#, example=false) get data from cached file
// func isCached(day#) checks if input have already been cached
// func saveToFile(data, day#, input/output) to save the data to a file
// func saveExampleToFile(data, day#) for example since it needs to skip past expected result
